import styled from 'styled-components';
import fonts from '../../styles/fonts';

const RedirectText = styled.h2`
  font-size: ${fonts.fsH5};
  /* font-weight: 400; */
`;

export default RedirectText;

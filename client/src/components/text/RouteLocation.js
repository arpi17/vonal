import styled from 'styled-components';
import fonts from '../../styles/fonts';

const RouteLocation = styled.span`
  display: inline-block;
  font-size: ${fonts.fsBodySmall};
`;

export default RouteLocation;

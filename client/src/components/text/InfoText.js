import styled from 'styled-components';
import fonts from '../../styles/fonts';
import colors from '../../styles/colors';

const InfoText = styled.span`
  font-size: ${fonts.fsBodySmall};
  color: ${colors.primary};
  cursor: pointer;
`;

export default InfoText;
